# Local development with the API running on port 8000.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
# Set false for a real API-only production build.
NEXT_PUBLIC_ENABLE_DEMO=true
# Used by the Next server/container to proxy relative /api requests in production.
API_INTERNAL_URL=
